@model IEnumerable<Entity.ProductTypeEntity>

@if (Model.Count() > 0)
{
    <ul class="sub-menu">
        @foreach (var item in Model)
        {
            <li><a href="/Product/List?productTypeId=@item.productTypeId#@item.productTypeId">@item.name</a></li>
        }
    </ul>
}


